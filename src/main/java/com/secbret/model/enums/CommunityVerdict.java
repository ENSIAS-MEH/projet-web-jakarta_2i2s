package com.secbret.model.enums;

/** Part III §9 CommunityVerdict (chk_community_verdict on scanned_url; column is nullable). */
public enum CommunityVerdict {
    UNKNOWN,
    BENIGN,
    SUSPICIOUS,
    MALICIOUS
}
