/**
 * Jakarta Mail (Angus Mail) email dispatch.
 * Contains email template builders and the low-level mail sender.
 * EmailService lives in service/ per the project structure spec.
 */
package com.secbret.email;
