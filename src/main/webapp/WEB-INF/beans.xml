<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
           https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0"
       bean-discovery-mode="annotated">
    <!--
        CDI bean discovery mode "annotated": only classes carrying a CDI scope
        annotation (@ApplicationScoped, @RequestScoped, @Singleton, etc.) are
        treated as beans. This avoids unintentional discovery of third-party
        classes and keeps the bean graph explicit and fast.
    -->
</beans>
